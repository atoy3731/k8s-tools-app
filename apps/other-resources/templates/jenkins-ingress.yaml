apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: jenkins-ingress
  namespace: kube-system
  annotations:
    cert-manager.io/cluster-issuer: demo-issuer
    {{- if .Values.httpsRedirect }}
    traefik.ingress.kubernetes.io/redirect-entry-point: https
    {{- end }}
spec:
  rules:
    - host: {{ .Values.jenkinsHost }}
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Values.jenkinsService.name }}
              servicePort: {{ .Values.jenkinsService.port }}
  tls:
  - hosts:
    - {{ .Values.jenkinsHost }}
    secretName: {{ .Values.jenkinsTlsSecret }}


